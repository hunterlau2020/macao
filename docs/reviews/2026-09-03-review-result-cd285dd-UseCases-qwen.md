# 全量用例体系（UseCases）PRD v2.5 对齐复审（基线 `cd285dd`）评审结论

- **评审日期**：2026-09-03
- **评审人**：qwen（独立评审）
- **评审对象**：`2026-09-03-review-request-cd285dd-UseCases-v2.5-Alignment.md`，钉死 `cd285dd`
- **结论**：**授予用例文档体系 L1 DOC-ALIGNED / PG-0（轨 B，连续第二轮）。** 本人 a0123e8 轮 A-2（UC-6 示例缺 `vote_result_ref`）实质闭环；用例正文对 PRD v2.5 权威表、契约、事实源的一致性保持无回退；无新 P0/P1。
- **结构化 issue**：`BLOCKING` × 0、`ADVISORY` × 3（P2×1 / P3×2）

## 1. 申请点名项与前序遗留核验

| 项 | 独立复验 | 判定 |
|---|---|---|
| UC-6 示例补齐 `vote_result_ref`（本人 A-2 / codex P1-2 跨轨部分） | `UC6-issue-triage-rework.md:44-47` 示例含 `vote_result_ref{path, evidence_commit, sha256}`；该示例过 `review_disposition` 契约（含本轮收紧的顶层 required）；与 PRD §2.5 :676 示例、提案 §4.2 三方一致 | ✅ CLOSED |
| D-6 纯整数门禁在用例的表述 | UC-5 :76/87、UC-10 体检项与本轮语义校验（`schema.py:113-157`）同文；本人 a0123e8 A-1"承诺半可执行"问题随配置期语义校验落地而**全可执行**（权重/独裁帽/双 quorum/胜方席位实测拒绝非法、接受合法） | ✅ 升级闭环 |
| 13 份用例机器自洽性 | 字节级控制字符扫描 13/13 clean；内嵌示例经仓库验证器复跑通过；`remote_name: null` 纯本地、D-1 不可变、D-2 单写者、UC-9 超时隔离表述与权威基准逐一抽验无矛盾 | ✅ |
| 符号链接 | `docs/usecases -> usercases` 在位 | ✅ |

## 2. 回归抽验（无回退确认）

- **UC-7**：五选项闭合、E7 源态 `CONSENSUS_CHECK` 系、`admin_override.json` 不可变语义——与本轮收敛后的提案 §4.5/清单同文（"或 REWORK"残留已清，用例侧本就干净）✓
- **UC-8**：六道关卡与 PRD §14.5 双分支（远端 `ls-remote` fail-closed / 纯本地）语义同序 ✓
- **UC-5**：DEADLOCK 即时落盘 + 五重门禁公式与 §2.3 逐项一致 ✓
- **机验**：93/93 OK、`compileall` 0、fixtures 10/10 + 20/20（3 份语义反例经 `test_schema.py:344-346` 接线）、双 Schema 目录零漂移——申请 §3 机验本机复跑成立

## 3. ADVISORY（P2/P3）

- **A-1（P2，跨轨牵连）**：设计同步轨根 `macao.yaml` 被语义门禁拒绝（见轨 A 报告 P1-1）——用例文档本身无矛盾，但"配置单一事实源"失效期间 `init`/`doctor` 类用例的消费方无合法活配置可取；轨 A 单点修复后即解除
- **A-2（P3）**：UC-8 六关卡与 §14.5 五步编号映射注记（连续四轮登记，语义无歧义）
- **A-3（P3）**：93/93 为 L1 文档轮机验，不得外推为 L2 代码证明（与 grok 同口径；codex 三项代码层差距登记于轨 A 报告 A-1）

## 4. 定级意见

**授予轨 B L1 DOC-ALIGNED / PG-0。** 判据核对：13 份用例与权威基准字段级可对照、无矛盾、无回退；全部示例可解析过契约；P0/P1 为零；本人前序全部用例轨遗留清零。与面板关系：grok 同授（本轨第 2 张授予票，累计 claude/grok/qwen 三方连续两轮一致）；codex REJECT 的三项证据均为代码实施差距（轨 A 交付物与 L2 域），除已闭环的 UC-6 示例外不涉用例正文——分歧如实登记。注意：轨 A 未授予，Phase 1 实施准入仍以该轨（根配置单点修复）为前提。

## Reviewer 自审记录

- 本轮对"用例表述 ↔ 语义校验实现"做了升级核验：a0123e8 登记的"承诺半可执行"项，本轮以合法/非法双侧探针确认转为全可执行后才改判闭环，未因申请声明直接销账
- UC-6 示例为逐行比对（含 sha256 占位格式），非转引
- 93/93、20/20、零漂移均本机复跑；未采信申请粘贴值；未覆盖：win32、Phase 1 实现
